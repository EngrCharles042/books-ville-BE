# ================================================================================
# - GENERAL PROPERTIES
# ================================================================================
server.port = 8080
server.servlet.context-path=/api/v1
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

# ================================================================================
# - POSTGRESQL DATABASE / DATASOURCE
# ================================================================================
spring.datasource.url = jdbc:postgresql://localhost:5432/booksVille
spring.datasource.username = postgres
spring.datasource.password = password

# ================================================================================
# - JPA / HIBERNATE
# ================================================================================
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.format = true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===========================================================
# - CLOUDINARY
# ===========================================================


# ===========================================================
# - MAIL SENDER
# ===========================================================

spring.mail.port=587
spring.mail.host=smtp.gmail.com
spring.mail.password=pamsmxdmlftrbhqe
spring.mail.properties.mail.smtp.auth=true
spring.mail.username=ng.booksville@gmail.com
spring.mail.properties.mail.smtp.starttls.enable=true